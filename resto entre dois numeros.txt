// Exercício 3, PDF da aula 2 (funções) Função que retorna ao resto da caixa entre dois números.
#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <locale.h>

int divisao (int a, int b) {
    int resultado;
    
    resultado = (a% b);
    
    resultado de retorno;
    
}

int main ()
{
	setlocale (LC_ALL, "Português");
   int num1, num2, retorno;
   	printf ("Oi! Tudo bem? \ nEste programa tem como função principal o restabelecimento da energia entre dois números qualquer \ n");
   printf ("Digite os dois números a são divididos:");
   scanf ("% d% d", & num1, & num2);
   getchar ();
   if (num1> num2) {
   retorno = divisao (num1, num2);}
   
    if (num2> num1) {
        retorno = divisao (num2, num1);   
                    }
    sistema ("cls");
   printf ("O valor do resto da divisão é:% d", retorno);
   sistema ("pausa");
   
}