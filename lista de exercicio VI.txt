/ * 8. Escrever no espaço o algoritmo para ler e imprimir dois números inteiros positivos.
Através de funções, demonstre:
- Se o primeiro numero para positivo, imprima o seu quadrado;
- veja o número para maiores que 10 e menor que 100, imprimir uma mensagem:
“Número está entre 10 e 100 - intervalo permitido”;
- Se o segundo número for menor que o primeiro, calcular e imprimir a diferença
do que o número de 1
* /

#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

	int caso1 (int a, int b) {
		if (a> 0) {
		
		printf ("A condição 1 diz -Se o primeiro numero para positivo, imprima seu quadrado \ n");
		printf ("->% d \ n", (a * a));}
		sistema ("pausa");
	}
	int caso2 (int a, int b) {
		
		printf ("A condição 2 diz-se o segundo número para maior que 10 e menor que 100", imprimir uma mensagem: \ n "Número está entre 10 e 100 intervalos permitidos" \ n ");
		
		if (b> 10 && b <100) {
		printf ("\ n -> O seugro número é% d, portanto:" Número está entre 10 e 100 - intervalo permitido ”\ n", b);
		}outro {
			printf ("\ n -> O seugndo número é% d, e não está intervalado, portanto não será exibida a mensagem! \ n", b);
		}
		sistema ("pausa");
	}
	int caso3 (int a, int b) {
		printf ("" O que é o seu número para o primeiro, o maior e o diferente do que o poder dela, mas o primeiro número adicionado de 1 \ n ");
		if (b <a) {
			printf ("A diferença entre% de% d e% d \ n", a, b, (ab));
		} else if (a <b) {
			printf ("\ n% d é menor que% d, pelo enunciado temos a segunda condição: \ n", a, b);
			printf ("\ n ->% d \ n", (a + 1));
		}
	}
	
	a Principal(){
		
	setlocale (LC_ALL, "Português");
		int num1, num2;
	printf ("\ n \ n8. Escrever no espaço o algoritmo para ler e imprimir dois números inteiros positivos.Trave de funções, demonstre: \ n- Se o primeiro número para positivo, imprima seu quadrado; \ n- Se o segundo número for maior que 10 e 100 menor, 100, imprimir uma mensagem: “Número está em 10 e 100 - intervalo permitido”; logo, verso do cartão de crédito, primeiro sucesso do € 1 de \ n \ n ");
	getchar ();
	sistema ("cls");
	

		printf ("Digite o valor para o primeiro número");
		scanf ("% d", & num1);
		printf ("\ n");
		getchar ();
		fflush (stdin);
		printf ("Digite o valor de um para o segundo número");
		scanf ("% d", & num2);
		printf ("\ n");
		sistema ("cls");
		
		caso1 (num1, num2);
			sistema ("cls");
		caso2 (num1, num2);
			sistema ("cls");
		caso3 (num1, num2);
		sistema ("cls");
		sistema ("pausa");
}